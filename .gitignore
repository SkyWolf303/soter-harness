# OS
.DS_Store

# Node
node_modules/
*.log

# Soter private runtime checkpoints (never portable configuration)
.soter/state/

# Claude Code — personal / runtime (never shared)
.claude/settings.local.json
.claude/evals/logs/*.jsonl
.claude/scheduled_tasks.lock

# Local planning tools (workshop scratch, never harness content)
nimbalyst-local/
.claude/worktrees/
